services:
  server:
    build:
      context: .
    ports:
      - "80:80" # HTTP
      - "443:443" # HTTPS
      - "443:443/udp" # HTTP/3
    volumes:
      #- ./:/app/public
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:

